---
description: Implement a renderer UI or interaction change.
allowed-tools: Read, Edit, MultiEdit, Grep, Glob, Bash
---

Read `AGENTS.md`, `docs/agent/project-map.md`, `docs/agent/workflows.md`, and `docs/agent/verification.md`.

Requested UI change:

`$ARGUMENTS`

Inspect `index.html`, `renderer.js`, and `style.css` together. Preserve control IDs and verify syntax. Launch the app only when visual validation is needed.
