---
description: Implement an AI default, context, payload, or row-AI behavior change.
allowed-tools: Read, Edit, MultiEdit, Grep, Glob, Bash
---

Read `AGENTS.md`, `docs/agent/ai-behavior.md`, `docs/agent/workflows.md`, and `docs/agent/verification.md`.

Requested AI change:

`$ARGUMENTS`

Implement the change across all affected layers. Run `npm.cmd run syntax` and `npm.cmd run check`. Report changed files and verification.
