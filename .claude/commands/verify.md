---
description: Run the repo verification checklist.
allowed-tools: Read, Bash
---

Read `docs/agent/verification.md`.

Verification request:

`$ARGUMENTS`

Run the appropriate commands. For code changes, default to `npm.cmd run syntax` and `npm.cmd run check`. Summarize results and failures.
